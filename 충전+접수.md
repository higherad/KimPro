# kimpro/reception.html — 비즈핏 자동 입금완료 + 캐시차감 이식 계획

higher/접수관리.html·정산관리.html에 있는 두 기능을 kimpro/reception.html에도 넣을 수 있는지 조사한 결과.
**아직 적용 안 함** — 스니펫과 순서만 정리.

- 기능 A: 충전 성공 시 그 대행사의 접수(status=1) 건을 비즈핏 충전,입금관리에서 자동으로 입금완료 처리
- 기능 B: 접수 승인(캐시차감 상품 포함) 시 비즈핏 파트너 캐시 자동 차감 + 충전,입금관리 메모 누적

결론: **가능하지만 그대로 복붙은 안 됨.** kimpro는 계정 모델이 higher와 달라서(아래 참고) 선행 스키마 변경이 필요하고,
게이트 로직도 kimpro 전용 예외(`isRequeue`)가 있어서 손대는 지점을 정확히 골라야 함.

---

## 1. 구조적 차이 (higher ↔ kimpro)

| 항목 | higher | kimpro |
|---|---|---|
| 충전 화면 | 별도 페이지(`정산관리.html`), 대행사별 셀이 여러 개 있는 표를 일괄 처리 | `reception.html` 안 모달(`doCharge()`), **계정 1개씩 개별 충전** — 훨씬 단순함 |
| 비즈핏 파트너 uid 매핑 | `ha/users/{key}.bizfitUid` (회원 계정당 1개) | **없음** — 새로 만들어야 함 |
| BizFit 로그인 ID/PW | `ha/bizfit_charge/{agencyId}` | `kimpro/charge_accounts/{username}/{key}` = `{agency, id, pw, unitPrice}` — `fetchBizfitAccount()`(4656행)가 이미 이 구조로 조회 중 |
| 단가(unitPrice) | `ha/users/{key}.unitPrice` (회원 단위) | 위 charge_accounts의 `unitPrice` 필드 (대행사 단위) — 구조가 달라 오히려 더 직접적 |
| 승인 시 deductAmount/yupflAmount 저장 | `HA.approveSlot(key, {...deductFieldsFor, ...yupflFieldsFor})` | **저장 안 함** — `HA.approveSlot(key)`를 인자 없이 호출 (2885/5239/5567/5719행). `deductFieldsFor`/`yupflFieldsFor` 같은 재사용 함수 자체가 없고, 계산 로직이 `buildNormalRows`/유입플 빌더 안에 인라인으로만 있음 |
| 게이트 로직 | 오늘 세션에 슬롯별 `requiredLegsByKey` 판정으로 재작성됨(교집합 방식 폐기) | **아직 옛날 방식(교집합)** — 게다가 `isRequeue` 슬롯 우회 예외가 별도로 있음(5221행 근처 주석 참고). higher 최신 게이트를 그대로 옮기면 이 예외가 깨짐 |
| `callBizfitProxy`/`BIZFIT_PROXY` | 있음 | **이미 있음**(4649/4666행) — 그대로 재사용 가능 |

**먼저 해야 하는 것 (선행 작업)**:
1. `kimpro/charge_accounts/{username}/{key}`에 `bizfitUid` 필드 추가 + 계정 설정 모달에 입력칸 추가.
2. `deductFieldsFor`/`yupflFieldsFor` 같은 슬롯 단위 계산 함수를 kimpro에도 뽑아내기(지금은 `buildNormalRows`/유입플 빌더 안에 뒤섞여 있음) — 승인 시 `HA.approveSlot(key, extra)`로 넘길 값이 필요하기 때문.

---

## 2. 스니펫 A — 계정 설정 모달에 bizfitUid 추가

`saveAccountModal`/`saveEditAcct`가 저장하는 객체에 필드 하나만 추가하면 됨. HTML 입력칸도 같이.

```js
// window.saveAccountModal (기존 unitPrice 옆에 bizfitUid 추가)
window.saveAccountModal = async function() {
  const agency    = document.getElementById('acct-agency').value.trim();
  const id        = document.getElementById('acct-id').value.trim();
  const pw        = document.getElementById('acct-pw').value.trim();
  const unitPrice = Number(document.getElementById('acct-unitprice').value) || 0;
  const bizfitUid = document.getElementById('acct-bizfituid').value.trim(); // 신규
  if (!id || !pw) { alert('아이디와 비밀번호를 모두 입력해주세요.'); return; }
  const cu  = HA.getCurrentUser();
  const key = agency ? safe(agency) : `acct_${Date.now()}`;
  await HA.saveChargeAccount(cu.username, key, { agency, id, pw, unitPrice, bizfitUid });
  _chargeAccts[key] = { agency, id, pw, unitPrice, bizfitUid };
  renderAcctList();
  // ...기존 입력칸 초기화 로직 이어서
};
```

`editAcct`/`saveEditAcct`/표 렌더(`renderAcctList`)에도 동일하게 `bizfitUid` 칼럼 하나 추가 — higher의
`ha/users`처럼 "이름이 정확히 안 맞으면 자동 매칭 금지, 사용자에게 확인" 원칙 그대로 적용(참고:
[[접수 승인 시 비즈핏 파트너 캐시 자동 차감 연동]] 메모리 — "제이엔베스트" vs "제이엔" 같은 사고 있었음).

---

## 3. 스니펫 B — 충전 성공 시 자동 입금완료 처리 (`doCharge` 수정)

higher 정산관리.html의 `startSettleCharge`(1190~1212행)를 kimpro의 단일 계정 흐름에 맞게 단순화.
표 순회(`for cell of ...`)가 없어서 higher보다 짧음.

```js
const BIZFIT_DIRECT_PROXY = 'https://bizfit-direct-proxy-938928195180.asia-northeast3.run.app';
// (kimpro는 이미 BIZFIT_PROXY 상수가 있음 — 같은 서버라 새로 안 만들고 그거 그대로 써도 됨)

window.doCharge = async function() {
  // ...기존 코드 그대로 (amount, acct, agency 구하는 부분 안 건드림)...

  if (ok) {
    btn.textContent = '⏳ 입금완료 처리 중...';
    resultEl.textContent = '충전 완료 — 입금완료 자동 처리 중...'; resultEl.style.color = 'var(--info)';

    // 방금 넣은 충전 신청 건(영업점+금액 일치)을 비즈핏 충전,입금관리에서 접수→입금완료로 자동 확정.
    // 서버가 금액 불일치/모호하면 아무 것도 안 건드리고 실패 반환 — 여기선 결과에 따라 표시만 다르게.
    let confirmOk = false, confirmMsg = '';
    try {
      const cres = await fetch(`${BIZFIT_DIRECT_PROXY}/bizfit-charge-confirm`, {
        method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ company: agency, amount, newStatus: '3' }),
      });
      const cjson = await cres.json();
      confirmOk = !!cjson.success; confirmMsg = cjson.message || '';
    } catch (e) { confirmMsg = `서버 연결 실패: ${e.message}`; }

    if (confirmOk) {
      btn.textContent = '✅ 완료';
      resultEl.textContent = `${msg} · 입금완료 처리됨`; resultEl.style.color = 'var(--accent)';
    } else {
      btn.textContent = '✅ 완료';
      resultEl.textContent = `${msg} · ⚠️ 입금완료 자동처리 실패 — 비즈핏에서 수동 확인 필요: ${confirmMsg}`;
      resultEl.style.color = 'var(--danger)';
    }
    setTimeout(function() { btn.disabled = false; btn.textContent = '⚡ 충전'; }, 3000);
  } else {
    // ...기존 실패 처리 그대로...
  }
};
```

**주의**: `company`엔 비즈핏 파트너 어드민에 등록된 정확한 영업점명이 들어가야 함(`agency` 변수 — 계정
설정에 저장된 값). 오타/공백 차이 있으면 매칭 실패로 배지만 ⚠️ 뜨고 캐시는 정상 충전된 상태로 남음 —
이 자체는 안전한 실패(아무것도 안 건드림)라 문제 없음.

---

## 4. 스니펫 C — 캐시차감 핵심 함수 (higher `접수관리.html` 1965~2075행 기준 이식)

kimpro는 `userBizfitUidMap`/`userBizfitUidReady`(회원 단위) 대신 **대행사(agencyId) 단위로 바로** 매핑을
조회하면 됨 — `HA.getChargeAccounts(cu.username)`이 위 스니펫 A로 넣은 `bizfitUid`까지 이미 갖고 있음.

```js
// ── 비즈핏 캐시 자동 차감 (higher 접수관리.html 이식, 2026-08-11 기준) ──────────

let _bizfitAgencyUidMap = null; // agencyId -> bizfitUid, 1회 로드 후 재사용
async function getBizfitAgencyUidMap() {
  if (_bizfitAgencyUidMap) return _bizfitAgencyUidMap;
  const cu = HA.getCurrentUser();
  const accts = await HA.getChargeAccounts(cu.username).catch(() => ({}));
  const map = new Map();
  Object.values(accts || {}).forEach(a => { if (a.agency && a.bizfitUid) map.set(a.agency, a.bizfitUid); });
  _bizfitAgencyUidMap = map;
  return map;
}

// higher와 동일 — 대행사(bizfitUid) 단위로 금액 합쳐서 /bizfit-refund 1회 호출.
// bizfitDeductAttempted를 미리 쓰면 성공 시에도 배지가 깜빡이는 문제가 있었음(2026-08-11 higher에서
// 발견) — 성공 땐 attempted+deducted를 한 번에, 실패 때만 attempted 단독으로 쓸 것.
async function settleBizfitCashBatch(bizfitUid, slots, deductAmount, yupflAmount) {
  const targets = slots.filter(s => s && !s.bizfitDeducted);
  if (!targets.length) return true;
  const amount = deductAmount + yupflAmount;
  if (amount <= 0) return true;
  try {
    const res = await callBizfitProxy('/bizfit-refund', { uid: bizfitUid, amount });
    if (res && res.success) {
      await Promise.all(targets.map(s => HA.updateSlot(s._key, { bizfitDeductAttempted: true, bizfitDeducted: true })));
      targets.forEach(s => { s.bizfitDeductAttempted = true; s.bizfitDeducted = true; });
      const agencyId = targets[0].agencyId;
      callBizfitProxy('/bizfit-charge-memo', { company: agencyId, yupflAmount, deductAmount })
        .then(mres => { if (!mres || !mres.success) console.error('비즈핏 충전 메모 실패:', agencyId, mres && mres.message); })
        .catch(e => console.error('비즈핏 충전 메모 오류:', agencyId, e));
      return true;
    }
    await Promise.all(targets.map(s => HA.updateSlot(s._key, { bizfitDeductAttempted: true })));
    targets.forEach(s => { s.bizfitDeductAttempted = true; });
    console.error('비즈핏 캐시 차감 실패:', targets.map(s => s._key), bizfitUid, amount, res && res.message);
    return false;
  } catch (e) {
    await Promise.all(targets.map(s => HA.updateSlot(s._key, { bizfitDeductAttempted: true })));
    targets.forEach(s => { s.bizfitDeductAttempted = true; });
    console.error('비즈핏 캐시 차감 오류:', targets.map(s => s._key), bizfitUid, amount, e);
    return false;
  }
}

async function settleBizfitCash(slot, fields) {
  if (!slot) return true;
  const map = await getBizfitAgencyUidMap();
  const bizfitUid = map.get(slot.agencyId);
  if (!bizfitUid) return true; // 매핑 없는 대행사는 조용히 스킵
  return await settleBizfitCashBatch(bizfitUid, [slot], fields.deductAmount || 0, fields.yupflAmount || 0);
}

// 목록 배지 판정 — bizfitDeducted만 보면 이 기능 이전 레거시 active 슬롯까지 오탐함(higher에서 실제로
// 겪은 사고, [단독]트렌드 캠페인 전량 도배) — bizfitDeductAttempted를 반드시 같이 확인.
function bizfitDeductPending(s) {
  return !!(s && s.status === 'active' && s.bizfitDeductAttempted && !s.bizfitDeducted);
}

window.retryBizfitDeduct = async function(key, btn) {
  if (btn) { btn.disabled = true; btn.textContent = '재시도 중...'; }
  const slot = allSlots.find(s => s._key === key);
  if (!slot) return;
  await settleBizfitCash(slot, { deductAmount: slot.deductAmount || 0, yupflAmount: slot.yupflAmount || 0 });
  renderReceipts(false);
};

// 승인(active 전환)만 처리하고 차감 대상을 대행사별로 묶어 반환(실행 안 함) — 팝업 자동승인 등에서 재사용.
// ※ deductFieldsFor/yupflFieldsFor는 kimpro에 아직 없음 — buildNormalRows/유입플 빌더의 인라인 계산을
//   슬롯 1개 기준으로 뽑아내는 작업이 선행돼야 함(§1 참고).
async function approveReadySlots(keys, slotMap) {
  if (!keys.length) return [];
  const fieldsByKey = new Map(keys.map(k => [k, { ...deductFieldsFor(slotMap.get(k)), ...yupflFieldsFor(slotMap.get(k)) }]));
  await Promise.all(keys.map(k => HA.approveSlot(k, fieldsByKey.get(k))));
  keys.forEach(k => { const s = slotMap.get(k); if (s) s.status = 'active'; });

  const map = await getBizfitAgencyUidMap();
  const byUid = new Map();
  for (const k of keys) {
    const s = slotMap.get(k);
    if (!s || s.bizfitDeducted) continue;
    const bizfitUid = map.get(s.agencyId);
    if (!bizfitUid) continue;
    const fields = fieldsByKey.get(k);
    const amount = (fields.deductAmount || 0) + (fields.yupflAmount || 0);
    if (amount <= 0) continue;
    if (!byUid.has(bizfitUid)) byUid.set(bizfitUid, { bizfitUid, slots: [], ded: 0, yup: 0 });
    const g = byUid.get(bizfitUid);
    g.slots.push(s);
    g.ded += fields.deductAmount || 0;
    g.yup += fields.yupflAmount || 0;
  }
  return [...byUid.values()];
}

// 승인 + 차감을 이어서 실행 — 실제 클릭에서 나온 호출 전용(자동 실행 금지, 아래 §6 참고).
async function approveSlotsWithBizfit(keys, slotMap) {
  const groups = await approveReadySlots(keys, slotMap);
  const results = await Promise.all(groups.map(async g => {
    const ok = await settleBizfitCashBatch(g.bizfitUid, g.slots, g.ded, g.yup);
    return { ...g, ok };
  }));
  return results.filter(r => !r.ok).map(({ ok, ...g }) => g);
}
```

---

## 5. 적용 지점 — 기존 `HA.approveSlot()` 호출 4곳

kimpro 게이트는 higher 최신본과 달리 **교집합 방식 + `isRequeue` 우회**가 그대로 살아있음. `approveSlotsWithBizfit`로
바꿀 때 이 우회 로직까지 같이 옮겨야 함 — 무작정 higher 함수로 치환하면 재접수 우회가 깨짐.

| 위치 | 현재 | 바꿀 방향 |
|---|---|---|
| 2885행 (`window.approveOne`류, 키워드변경 취소/승격) | `await HA.approveSlot(key);` | 단건이므로 `await settleBizfitCash(slot, extra)`로 이어붙이되, `extra`(deductAmount/yupflAmount)를 어디서 구할지 먼저 정해야 함(§1) |
| 5239행 (게이트 `tryApprove`) | `Promise.all(keys.map(k => HA.updateSlot(k, {status:'active'})))` | `approveSlotsWithBizfit(keys, this.slotMap)`로 교체 — **단, isRequeue 우회로 골라진 `keys` 그대로 넘기면 됨(우회 로직 자체는 안 건드림)** |
| 5567행 (유입플 단일완료 else분기) | `Promise.all(allDoneSlotKeys.map(k => HA.approveSlot(k)))` | `approveSlotsWithBizfit(allDoneSlotKeys, slotMap)` |
| 5719행 (`finalizeSlotApprovals`, directApprove) | `Promise.all(directApprove.map(k => HA.approveSlot(k)))` | `approveSlotsWithBizfit(directApprove, slotMap)` |

모든 지점에서 반환된 `failed`(실패 그룹)를 **higher처럼 팝업 안 배너로 보여줄지, 아니면 처음부터 목록 배지만
쓸지** 결정 필요 — higher는 배너(팝업 세션 한정)+배지(마커 기반, 영구) 두 겹으로 감. kimpro는 처음부터
배지만 먼저 넣고 배너는 나중에 추가해도 됨(둘 다 마커 기반이라 순서 상관없음).

---

## 6. 자동 실행 여부 — higher가 오늘 겪은 결론 그대로 적용 권장

higher에서 "팝업 열자마자 자동으로 승인/차감"을 두고 여러 번 뒤집힌 끝에 정착한 최종 원칙:

- **승인(상태 전환, `HA.approveSlot`)은 팝업 열 때 자동으로 처리해도 안전** — 내부 RTDB 쓰기라 실패해도 잃을
  게 없음(재시도하면 그만).
- **캐시차감(`/bizfit-refund`, 외부 API·실제 돈)은 절대 자동 실행 금지** — "접수 신청"/"재시도" 클릭이 있을
  때만 실행. 실패는 배지/배너로만 보여주고 조용히 재시도하지 않음.

kimpro 이식할 때도 이 원칙 그대로 가져가면 higher가 겪은 시행착오(자동실행→수동버튼→자동→제거, 4~5번
뒤집힘)를 반복 안 해도 됨.

---

## 7. 그 밖에 겪었던 함정 (kimpro 이식 시 미리 피할 것)

- **fire-and-forget 금지**: `settleBizfitCashBatch`/`settleBizfitCash` 호출 앞에 반드시 `await` — 안 그러면
  관리자가 승인 직후 페이지 벗어날 때 차감 요청이 조용히 취소될 수 있음.
- **동시 클릭 이중 차감 방지**: 승인 버튼은 클릭 즉시 `disabled`부터 시킬 것 — `bizfitDeducted` 체크만으로는
  거의 동시에 두 요청이 겹치면 둘 다 통과해버림.
- **`/bizfit-refund`의 memo 파라미터는 안 씀** — 서버가 항상 빈 문자열로 제출. 사유 기록은
  `/bizfit-charge-memo` 전용(§3 스니펫에 이미 반영됨).
- **메모는 합산 대신 줄 추가**: `/bizfit-charge-memo`는 서버 쪽에서 이미 "기존 메모 다음 줄에 추가"로
  고쳐져 있음(2026-08-11) — kimpro 쪽에서 별도 처리 불필요, 그대로 호출만 하면 됨.
- **대행사명 자동 매칭 금지**: `agencyId`가 비즈핏 파트너 어드민 회사명과 글자 하나까지 정확히 같아야
  함 — 다르면 사용자에게 같은 계정인지 먼저 확인.

---

## 8. 제안 순서

1. `kimpro/charge_accounts`에 `bizfitUid` 필드 추가(스니펫 A) — 계정 몇 개 실제로 매핑해서 데이터부터 채움.
2. `deductFieldsFor`/`yupflFieldsFor` 추출(§1) — 이게 없으면 §4 스니펫이 안 돌아감.
3. §4 함수 세트 붙이고, §5의 4개 지점 중 **하나만**(예: 유입플 단일완료) 먼저 연결해서 실사용으로 검증.
4. 문제없으면 나머지 3곳 확장.
5. §3(충전 자동 입금완료)은 독립적이라 아무 때나 먼저 해도 무방 — 오히려 이게 더 간단해서 먼저 해보는 것도 방법.
