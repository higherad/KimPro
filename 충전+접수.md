# kimpro/reception.html — 비즈핏 자동 입금완료 + 캐시차감 이식 계획

higher/접수관리.html·정산관리.html에 있는 두 기능을 kimpro/reception.html에도 넣을 수 있는지 조사한 결과.
**아직 적용 안 함** — 스니펫과 순서만 정리.

- 기능 A: 충전 성공 시 그 대행사의 접수(status=1) 건을 비즈핏 충전,입금관리에서 자동으로 입금완료 처리
- 기능 B: 접수 승인(캐시차감 상품 포함) 시 비즈핏 파트너 캐시 자동 차감 + 충전,입금관리 메모 누적

결론: **가능하고, 생각보다 간단함** — 새 필드가 필요 없다는 게 확인됐음(§2). 다만 게이트 로직은 kimpro
전용 예외(`isRequeue`)와 "재오픈 시 자동 승인" 로직이 남아있어서 손대는 지점을 정확히 골라야 함.

---

## 1. 구조적 차이 (higher ↔ kimpro)

| 항목 | higher | kimpro |
|---|---|---|
| 충전 화면 | 별도 페이지(`정산관리.html`), 대행사별 셀이 여러 개 있는 표를 일괄 처리 | `reception.html` 안 모달(`doCharge()`), **계정 1개씩 개별 충전** — 훨씬 단순함 |
| 비즈핏 파트너 uid | `ha/users/{key}.bizfitUid` (별도 필드) | **별도 필드 불필요** — §2 참고, 충전 계정의 `id`를 그대로 재사용 |
| BizFit 로그인 ID/PW | `ha/bizfit_charge/{agencyId}` | `kimpro/charge_accounts/{username}/{key}` = `{agency, id, pw, unitPrice}` — `fetchBizfitAccount()`(4656행)가 이미 이 구조로 조회 중 |
| 단가(unitPrice) | `ha/users/{key}.unitPrice` (회원 단위) | 위 charge_accounts의 `unitPrice` 필드 (대행사 단위) — `getYupflUnitPriceMap()`(5012행)이 이미 agencyId→unitPrice로 조회 중 |
| 승인 시 deductAmount/yupflAmount 저장 | `HA.approveSlot(key, {...deductFieldsFor, ...yupflFieldsFor})` | **저장 안 함** — `HA.approveSlot(key)`를 인자 없이 호출(2885/5239/5567/5719행). §4에서 추가 |
| 게이트 로직 | 오늘 세션에 슬롯별 `requiredLegsByKey` 판정으로 재작성됨(교집합 방식 폐기) | **옛날 방식(교집합) 그대로** + `isRequeue` 슬롯 우회 예외(higher엔 없는 kimpro 전용 로직, 5220행 근처). **이번 포팅에서 이 게이트 자체를 갈아엎지 않음** — isRequeue 우회를 건드리면 재접수 기능이 깨짐. §5에서 기존 게이트 구조를 유지한 채 승인/차감만 갈아끼움 |
| 승인 모달 열릴 때 자동 승인 | (오늘 세션 끝에) **차감은 절대 자동 실행 안 함, 승인(상태전환)만 자동** | `if (gate) gate.tryApprove();`(5249행)이 **지금은 승인만 하니 안전**하지만, tryApprove()에 차감을 얹으면 재오픈만 해도 캐시가 빠져나가는 문제가 그대로 재현됨 — §5에서 분리 처리 |
| `callBizfitProxy`/`BIZFIT_PROXY` | 있음 | **이미 있음**(4649/4666행) — 그대로 재사용 |
| 재접수(isRequeue) 슬롯의 차감 | 개념 자체가 없음(재접수 기능이 higher엔 없음) | **차감/메모 로직 대상에서 제외** — 순수 접수 건만 처리(§4-3, 사용자 확인) |

---

## 2. 비즈핏 uid — 새 필드 불필요, 기존 `id` 재사용 (실측 확인 완료)

**확인 결과**: `ha/users.bizfitUid`와 `ha/bizfit_charge.id`(BizFit 로그인 ID)를 대행사별로 실제 데이터
대조해봤더니, **19개 매핑 전체가 예외 없이 완전히 동일한 값**이었음(예: `trend@trend.com`,
`jnbest@jnbest.com` 등 — 전부 일치). 즉 `/bizfit-refund`가 요구하는 "uid"는 그 대행사의 BizFit 로그인
ID(이메일)와 같은 값 — 별도로 관리되는 게 아니라 애초에 같은 식별자였음.

**결론**: kimpro는 `bizfitUid`라는 새 필드를 만들 필요가 없음. `kimpro/charge_accounts/{username}/{key}.id`
(계정 설정에 이미 있는 "아이디" 칸, `fetchBizfitAccount()`가 이미 쓰고 있는 값)를 그대로 `/bizfit-refund`의
`uid` 파라미터로 넘기면 됨 — **DB 스키마도, 계정 설정 모달 UI도 손댈 필요 없음.**

```js
// §4-1에서 이렇게 씀 — a.bizfitUid가 아니라 a.id를 그대로 uid로 사용
_agencyBizfitUidMap.set(a.agency, a.id);
```

**주의**: `admin.bizfit.kr`(대행사 로그인) 계정과 `bfnew/partners/...`(파트너 어드민) 계정은 서버 쪽에서
서로 다른 로그인 세션(`BIZFIT_ID`/`PW` vs `BIZFIT_PARTNER_ID`/`PW`)을 쓰지만, 그 안에서 "이 대행사가
누구인지" 가리키는 식별자(이메일)는 같은 값을 공유한다는 뜻 — 두 시스템이 다른 로그인이라는 사실 자체는
안 바뀜, uid 값만 재사용 가능하다는 것.

**매핑 원칙**: 대행사명이 비즈핏 파트너 어드민 회사명과 글자 하나까지 정확히 같아야 함. 안 맞으면(예:
"제이엔베스트" vs "제이엔") 자동 매칭하지 말고 사용자에게 같은 계정인지 먼저 확인 — higher에서 실제로
이 문제로 매핑을 건너뛴 사례 있음([[접수 승인 시 비즈핏 파트너 캐시 자동 차감 연동]] 메모리 참고).

---

## 3. 충전 성공 시 자동 입금완료 처리 (`doCharge` 수정)

higher 정산관리.html의 `startSettleCharge`(1190~1212행)를 kimpro의 단일 계정 흐름에 맞게 단순화.
표 순회(`for cell of ...`)가 없어서 higher보다 짧음.

```js
const BIZFIT_DIRECT_PROXY = 'https://bizfit-direct-proxy-938928195180.asia-northeast3.run.app';
// (kimpro는 이미 BIZFIT_PROXY 상수가 있음 — 같은 서버라 새로 안 만들고 그거 그대로 써도 됨)

window.doCharge = async function() {
  // ...기존 코드 그대로 (amount, acct, agency 구하는 부분 안 건드림)...

  if (ok) {
    btn.textContent = '⏳ 입금완료 처리 중...';
    resultEl.textContent = '충전 완료 — 입금완료 자동 처리 중...'; resultEl.style.color = 'var(--info)';

    let confirmOk = false, confirmMsg = '';
    try {
      const cres = await fetch(`${BIZFIT_DIRECT_PROXY}/bizfit-charge-confirm`, {
        method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ company: agency, amount, newStatus: '3' }),
      });
      const cjson = await cres.json();
      confirmOk = !!cjson.success; confirmMsg = cjson.message || '';
    } catch (e) { confirmMsg = `서버 연결 실패: ${e.message}`; }

    if (confirmOk) {
      btn.textContent = '✅ 완료';
      resultEl.textContent = `${msg} · 입금완료 처리됨`; resultEl.style.color = 'var(--accent)';
    } else {
      btn.textContent = '✅ 완료';
      resultEl.textContent = `${msg} · ⚠️ 입금완료 자동처리 실패 — 비즈핏에서 수동 확인 필요: ${confirmMsg}`;
      resultEl.style.color = 'var(--danger)';
    }
    setTimeout(function() { btn.disabled = false; btn.textContent = '⚡ 충전'; }, 3000);
  } else {
    // ...기존 실패 처리 그대로...
  }
};
```

**주의**: `company`엔 비즈핏 파트너 어드민에 등록된 정확한 영업점명이 들어가야 함(`agency` 변수). 매칭
실패하면 배지만 ⚠️ 뜨고 캐시는 정상 충전된 상태로 남음 — 안전한 실패(아무것도 안 건드림)라 문제 없음.

---

## 4. 핵심 함수 세트 (deductFieldsFor/yupflFieldsFor 추출 포함)

### 4-1. 계정 정보 캐시 — unitPrice + uid(=id 재사용)를 한 번에

기존 `getYupflUnitPriceMap()`(5012행)은 매번 새로 `HA.getChargeAccounts()`를 fetch함 — 승인 로직에서도
같은 데이터가 필요하니, **캐싱되는 버전을 새로 하나 만들어 승인 관련 함수들이 공유**한다(기존
`getYupflUnitPriceMap` 호출부는 안 건드림, 새 캐시는 그것과 별개로 추가).

```js
// ── 승인 시 캐시차감용 계정 캐시 (agencyId -> unitPrice / bizfit uid) ──────────
// HA.getChargeAccounts()는 매번 fetch라, deductFieldsFor 등에서 반복 호출하면 낭비 —
// 1회 로드해서 재사용. userBizfitUidReady(higher)와 동일한 이유로 "로드 전에 승인하면
// 매핑 없음으로 오판"하는 레이스를 막기 위해 promise를 그대로 노출해 항상 await하고 씀.
// bizfitUid는 별도 필드가 아니라 a.id(BizFit 로그인 이메일)를 그대로 씀 — §2에서 실측 확인함.
let _agencyPriceMap    = new Map();
let _agencyBizfitUidMap = new Map();
const agencyAccountsReady = (async () => {
  try {
    const cu = HA.getCurrentUser();
    const accts = await HA.getChargeAccounts(cu.username);
    Object.values(accts || {}).forEach(a => {
      if (!a.agency) return;
      _agencyPriceMap.set(a.agency, Number(a.unitPrice) || 0);
      if (a.id) _agencyBizfitUidMap.set(a.agency, a.id);
    });
  } catch (e) { console.error('충전 계정 캐시 로드 실패:', e); }
})();
```

### 4-2. `deductFieldsFor`/`yupflFieldsFor` — higher와 100% 동일 로직, unitPrice 소스만 교체

higher(1911/1938행)는 `userPriceMap.get(slot.userId)`(회원 단위), kimpro는 `_agencyPriceMap.get(slot.agencyId)`
(대행사 단위)만 다르고 나머지 계산식은 그대로 복사 가능(`ap`/`yupflAlloc`/`yupflRuleFor` 전부 kimpro에 이미
동일 이름으로 있음, §1 표 참고).

```js
// 승인 시점에 차감 비율로 배분되는 수량/금액을 스냅샷으로 남김 — higher deductFieldsFor와 동일 로직
function deductFieldsFor(slot) {
  if (!slot) return {};
  const dp = ap().find(p => p.deductOnly);
  if (!dp) return {};
  const dTarget = yupflAlloc(slot.dailyTarget||0, dp.fixedPct);
  if (dTarget <= 0) return {};
  const unitPrice = (slot.unitPrice!=null && slot.unitPrice>0) ? slot.unitPrice : (_agencyPriceMap.get(slot.agencyId)||0);
  return { deductTarget: dTarget, deductAmount: dTarget * (slot.days||0) * unitPrice };
}

// 승인 시점에 유입플/분할로 배분되는 수량/금액을 스냅샷으로 남김 — higher yupflFieldsFor와 동일 로직
function yupflFieldsFor(slot) {
  if (!slot) return {};
  const yupfl = ap().find(p => p.id === 'yupfl' && p.fixedAlloc);
  const split = ap().find(p => p.id === 'yupfl2' && p.splitProduct);
  if (!yupfl && !split) return {};
  const deduct = ap().find(p => p.deductOnly);
  const base = slot.dailyTarget||0;
  const remaining = deduct ? Math.max(0, base - yupflAlloc(base, deduct.fixedPct)) : base;
  let target = 0;
  if (yupfl) {
    const workKw = (slot.yupflKeyword||'').trim();
    const rule   = yupflRuleFor(yupfl, workKw);
    target += Math.min(yupflAlloc(remaining, rule ? rule.fixedPct : yupfl.fixedPct), remaining);
  }
  if (split && Array.isArray(split.missionRules)) {
    split.missionRules.forEach(rule => {
      const workKw = ((slot.splitKeywords || {})[rule.mission] || '').trim();
      if (!workKw) return;
      target += Math.min(yupflAlloc(remaining, rule.fixedPct), remaining);
    });
  }
  if (target <= 0) return {};
  const unitPrice = (slot.unitPrice!=null && slot.unitPrice>0) ? slot.unitPrice : (_agencyPriceMap.get(slot.agencyId)||0);
  return { yupflTarget: target, yupflAmount: target * (slot.days||0) * unitPrice };
}
```

**주의**: 이 두 함수는 **동기 함수**(higher와 동일 호출 관례 유지 위해) — 그래서 `_agencyPriceMap`이 로드되기
전에 호출되면 unitPrice=0으로 계산될 위험이 있음. 이건 사실 higher도 갖고 있는 동일한 레이스(higher의
`userPriceMap`도 fire-and-forget으로 로드되고 deductFieldsFor 호출 전에 await하는 곳이 없음)라 kimpro만의
새 문제는 아니지만, 굳이 더 안전하게 하려면 페이지 최초 렌더(`renderReceipts` 초기 호출) 뒤에
`await agencyAccountsReady`를 한 번 넣어 첫 렌더가 이 캐시 로드를 기다리게 하면 됨.

### 4-3. 차감 실행 함수 — higher 접수관리.html 1965~2075행 이식 (재접수 슬롯 제외 포함)

**재접수(isRequeue) 슬롯은 차감/메모 로직 대상에서 제외** — 승인(상태 전환)은 기존 게이트 우회 로직대로
그대로 되지만, 캐시차감/메모는 **순수 접수 건만** 처리하도록 `approveReadySlots`의 그룹핑 단계에서
`s.isRequeue`인 슬롯을 걸러낸다(사용자 확인, 2026-08-11). 승인 자체를 막는 게 아니라 "차감 대상에서만 뺀다"는
점 주의 — isRequeue 슬롯도 승인(active 전환)은 정상적으로 일어남.

```js
// 대행사(bizfitUid) 단위로 금액 합쳐서 /bizfit-refund 1회 호출. bizfitDeductAttempted를 성공/실패
// 경로에서 언제 쓰는지는 higher가 겪은 깜빡임 버그(2026-08-11) 때문 — 성공 땐 attempted+deducted를
// 한 번에, 실패 때만 attempted 단독으로 쓴다(순서 바뀌면 배지가 잠깐 떴다 사라지는 깜빡임 생김).
async function settleBizfitCashBatch(bizfitUid, slots, deductAmount, yupflAmount) {
  const targets = slots.filter(s => s && !s.bizfitDeducted);
  if (!targets.length) return true;
  const amount = deductAmount + yupflAmount;
  if (amount <= 0) return true;
  try {
    const res = await callBizfitProxy('/bizfit-refund', { uid: bizfitUid, amount });
    if (res && res.success) {
      await Promise.all(targets.map(s => HA.updateSlot(s._key, { bizfitDeductAttempted: true, bizfitDeducted: true })));
      targets.forEach(s => { s.bizfitDeductAttempted = true; s.bizfitDeducted = true; });
      const agencyId = targets[0].agencyId;
      callBizfitProxy('/bizfit-charge-memo', { company: agencyId, yupflAmount, deductAmount })
        .then(mres => { if (!mres || !mres.success) console.error('비즈핏 충전 메모 실패:', agencyId, mres && mres.message); })
        .catch(e => console.error('비즈핏 충전 메모 오류:', agencyId, e));
      return true;
    }
    await Promise.all(targets.map(s => HA.updateSlot(s._key, { bizfitDeductAttempted: true })));
    targets.forEach(s => { s.bizfitDeductAttempted = true; });
    console.error('비즈핏 캐시 차감 실패:', targets.map(s => s._key), bizfitUid, amount, res && res.message);
    return false;
  } catch (e) {
    await Promise.all(targets.map(s => HA.updateSlot(s._key, { bizfitDeductAttempted: true })));
    targets.forEach(s => { s.bizfitDeductAttempted = true; });
    console.error('비즈핏 캐시 차감 오류:', targets.map(s => s._key), bizfitUid, amount, e);
    return false;
  }
}

async function settleBizfitCash(slot, fields) {
  if (!slot || slot.isRequeue) return true; // 재접수 슬롯은 차감 스킵
  await agencyAccountsReady;
  const bizfitUid = _agencyBizfitUidMap.get(slot.agencyId);
  if (!bizfitUid) return true; // 매핑 없는 대행사는 조용히 스킵
  return await settleBizfitCashBatch(bizfitUid, [slot], fields.deductAmount || 0, fields.yupflAmount || 0);
}

// 목록 배지 판정 — bizfitDeducted만 보면 이 기능 이전 레거시 active 슬롯까지 오탐함(higher가 실제로
// 겪은 사고, [단독]트렌드 캠페인 전량 도배) — bizfitDeductAttempted를 반드시 같이 확인.
function bizfitDeductPending(s) {
  return !!(s && s.status === 'active' && s.bizfitDeductAttempted && !s.bizfitDeducted);
}

window.retryBizfitDeduct = async function(key, btn) {
  if (btn) { btn.disabled = true; btn.textContent = '재시도 중...'; }
  const slot = allSlots.find(s => s._key === key);
  if (!slot) return;
  await settleBizfitCash(slot, { deductAmount: slot.deductAmount || 0, yupflAmount: slot.yupflAmount || 0 });
  renderReceipts(false);
};

// 승인(active 전환)만 처리하고 차감 대상을 대행사별로 묶어 반환(실행 안 함) — 팝업 자동승인 등에서 재사용.
// isRequeue 슬롯은 승인은 그대로 하되(HA.approveSlot은 전부에 대해 실행) 차감 그룹에는 안 넣음.
async function approveReadySlots(keys, slotMap) {
  if (!keys.length) return [];
  const fieldsByKey = new Map(keys.map(k => [k, { ...deductFieldsFor(slotMap.get(k)), ...yupflFieldsFor(slotMap.get(k)) }]));
  await Promise.all(keys.map(k => HA.approveSlot(k, fieldsByKey.get(k))));
  keys.forEach(k => { const s = slotMap.get(k); if (s) s.status = 'active'; });

  await agencyAccountsReady;
  const byUid = new Map();
  for (const k of keys) {
    const s = slotMap.get(k);
    if (!s || s.bizfitDeducted || s.isRequeue) continue; // 재접수 슬롯 제외
    const bizfitUid = _agencyBizfitUidMap.get(s.agencyId);
    if (!bizfitUid) continue;
    const fields = fieldsByKey.get(k);
    const amount = (fields.deductAmount || 0) + (fields.yupflAmount || 0);
    if (amount <= 0) continue;
    if (!byUid.has(bizfitUid)) byUid.set(bizfitUid, { bizfitUid, slots: [], ded: 0, yup: 0 });
    const g = byUid.get(bizfitUid);
    g.slots.push(s);
    g.ded += fields.deductAmount || 0;
    g.yup += fields.yupflAmount || 0;
  }
  return [...byUid.values()];
}

// 승인 + 차감을 이어서 실행 — 실제 클릭에서 나온 호출 전용(자동 실행 금지, §6 참고).
async function approveSlotsWithBizfit(keys, slotMap) {
  const groups = await approveReadySlots(keys, slotMap);
  const results = await Promise.all(groups.map(async g => {
    const ok = await settleBizfitCashBatch(g.bizfitUid, g.slots, g.ded, g.yup);
    return { ...g, ok };
  }));
  return results.filter(r => !r.ok).map(({ ok, ...g }) => g);
}
```

### 4-4. 목록에 배지 추가

kimpro reception.html의 목록 렌더 부분(상태 칼럼 — higher `접수관리.html`의 `stMap[s.status]` 자리에
해당하는 곳)에 higher와 동일하게 붙이면 됨:

```js
`${stMap[s.status]||s.status}${bizfitDeductPending(s) ? ` <button class="action-btn" style="background:#fef2f2;color:#b91c1c;padding:2px 6px;font-size:10px;" title="비즈핏 캐시 차감이 완료되지 않았습니다. 클릭 시 재시도합니다." onclick="retryBizfitDeduct('${s._key}', this)">⚠️ 차감 재시도</button>` : ''}`
```

---

## 5. 적용 지점 — 기존 `HA.approveSlot()` 호출 4곳 + 게이트 자동실행 1곳

### 5-1. `window.approveOne` (2874~2889행) — 단건 승인

```js
window.approveOne = async function(key) {
  const idx  = allSlots.findIndex(x => x._key === key);
  const live = (await HA.getDoc(`kimpro/slots/${key}`)).val() || {};
  if (live.status === 'change') {
    // 취소/원상복구 분기 — 안 건드림
    const target = live.preChangeStatus || 'active';
    await HA.updateSlot(key, { status: target, preChangeStatus: null });
    if (idx !== -1) { allSlots[idx].status = target; delete allSlots[idx].preChangeStatus; }
  } else {
    const slot = idx !== -1 ? allSlots[idx] : null;
    const extra = { ...deductFieldsFor(slot), ...yupflFieldsFor(slot) };
    await HA.approveSlot(key, extra);
    if (idx !== -1) allSlots[idx].status = 'active';
    await settleBizfitCash(slot, extra); // 추가 — 내부에서 isRequeue면 자동 스킵
  }
  closeModalAndRefresh();
};
```

### 5-2. 게이트 객체 (5194~5249행) — `readyKeys()` 분리 + `tryApprove()` 교체 + 자동실행 분리

기존 `tryApprove()`가 판정(`primary`/`rest`/`isRequeue` 필터)과 실행(raw `HA.updateSlot`)을 한 몸에
갖고 있던 걸, **판정만 `readyKeys()`로 분리**하고 실행은 `approveSlotsWithBizfit`에 위임. 그리고 **5249행의
`if (gate) gate.tryApprove();`(재오픈 시 자동 반영)를 승인 전용으로 바꿔서, 재오픈만 해도 캐시가 빠져나가는
일이 없게** 한다 — higher가 이 부분에서 여러 번 뒤집힌 끝에 정착한 결론(§6). isRequeue 우회 판정 자체는
그대로 두고(승인 대상 선정 로직은 안 건드림), 차감 제외는 §4-3의 `approveReadySlots`가 처리.

```js
const gate = (activeLegs.length > 1) ? {
  legs: activeLegs,
  normalKeys:  normalAlreadyDone.size  ? normalAlreadyDone  : null,
  fixedKeys:   fixedAlreadyDone.size   ? fixedAlreadyDone   : null,
  charlieKeys: charlieAlreadyDone.size ? charlieAlreadyDone : null,
  slotMap: new Map(allSlots.map(s => [s._key, s])),
  // 기존 tryApprove()의 판정 부분(primary/rest/isRequeue 우회)을 그대로 옮김 — 로직 변경 없음, 실행만 분리.
  readyKeys() {
    const primary = this.legs.includes('normalKeys')  ? 'normalKeys'
                   : this.legs.includes('charlieKeys') ? 'charlieKeys'
                   : this.legs[0];
    const rest = this.legs.filter(l => l !== primary);
    if (this[primary] === null) return [];
    return [...this[primary]].filter(k => {
      const s = this.slotMap.get(k);
      if (s && s.isRequeue) return true;
      return rest.every(l => this[l] && this[l].has(k));
    });
  },
  async tryApprove() {
    const keys = this.readyKeys();
    if (keys.length) await approveSlotsWithBizfit(keys, this.slotMap);
    renderReceipts(false);
  }
} : null;

// 재오픈 시 이미 조건 충족된 슬롯은 "승인(상태 전환)"만 자동 처리 — 내부 RTDB 쓰기라 안전.
// 차감(외부 API, 실제 돈)은 여기서 절대 실행 안 함 — 클릭(접수 신청/재시도)이 있을 때만 실행됨.
if (gate) {
  const ready = gate.readyKeys();
  if (ready.length) approveReadySlots(ready, gate.slotMap).then(() => renderReceipts(false));
}
```

### 5-3. 유입플 단일완료 (5563~5569행)

```js
if (gate) {
  gate.fixedKeys = new Set(allDoneSlotKeys);
  await gate.tryApprove();
} else {
  await approveSlotsWithBizfit(allDoneSlotKeys, slotMap); // 교체
}
```

### 5-4. 일반/찰리 `finalizeSlotApprovals` (5718~5721행)

`directApprove`(비게이트 또는 isRequeue 우회 슬롯)만 여기서 즉시 처리 — `gateDeferred`는 기존처럼
`approvedKeys`에 쌓여서 나중에 `gate.tryApprove()` 경로를 탐(이 부분 안 건드림). `directApprove`에
isRequeue 슬롯이 섞여 있어도 `approveSlotsWithBizfit`이 내부적으로 차감만 걸러내므로 그대로 넘기면 됨.

```js
if (directApprove.length) {
  await approveSlotsWithBizfit(directApprove, slotMap); // 교체
}
```

---

## 6. 자동 실행 원칙 (higher가 여러 번 뒤집힌 끝에 정착한 결론)

- **승인(상태 전환, `HA.approveSlot`)은 자동으로 처리해도 안전** — 내부 RTDB 쓰기라 실패해도 잃을 게 없음.
- **캐시차감(`/bizfit-refund`, 외부 API·실제 돈)은 절대 자동 실행 금지** — "접수 신청"/"재시도" 클릭이
  있을 때만 실행. 실패는 배지/배너로만 보여주고 조용히 재시도하지 않음.
- kimpro의 기존 `if (gate) gate.tryApprove();`(재오픈 자동실행)를 그대로 두고 `tryApprove()`에만 차감을
  얹으면, **재오픈만 해도 캐시가 빠져나가는 정확히 그 문제가 재현됨** — §5-2처럼 반드시 분리해야 함.

---

## 7. 그 밖에 겪었던 함정 (kimpro 이식 시 미리 피할 것)

- **fire-and-forget 금지**: `settleBizfitCashBatch`/`settleBizfitCash` 호출 앞에 반드시 `await`.
- **동시 클릭 이중 차감 방지**: 승인 버튼은 클릭 즉시 `disabled`부터 시킬 것.
- **`/bizfit-refund`의 memo 파라미터는 안 씀** — 서버가 항상 빈 문자열로 제출. 사유 기록은
  `/bizfit-charge-memo` 전용(§4-3에 이미 반영됨).
- **메모는 합산 대신 줄 추가**: `/bizfit-charge-memo`는 서버 쪽에서 이미 "기존 메모 다음 줄에 추가"로
  고쳐져 있음(2026-08-11) — kimpro 쪽에서 별도 처리 불필요, 그대로 호출만 하면 됨.
- **대행사명 자동 매칭 금지**: §2 참고.
- **재접수(isRequeue) 슬롯은 차감/메모 대상에서 제외**: §4-3 참고 — 승인은 되지만 캐시는 안 건드림.
- **배지 판정은 `bizfitDeducted`만 보지 말 것**: §4-3의 `bizfitDeductPending` 참고 — 반드시
  `bizfitDeductAttempted`와 같이 확인.

---

## 8. 제안 순서

1. §4-1/4-2 — 계정 캐시 + `deductFieldsFor`/`yupflFieldsFor` 추가(새 DB 필드나 UI 작업 없음, §2 참고).
2. §4-3/4-4 — 차감 함수 세트(재접수 제외 포함) + 배지 UI 추가.
3. §5 — 4개 지점 중 **하나만**(예: 유입플 단일완료, 5-3) 먼저 연결해서 실사용으로 검증.
4. 문제없으면 나머지(5-1, 5-2, 5-4) 확장.
5. §3(충전 자동 입금완료)은 독립적이라 아무 때나 먼저 해도 무방 — 오히려 이게 더 간단해서 먼저 해보는
   것도 방법.
